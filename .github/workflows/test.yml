name: 'test'

on:
  push

jobs:
  test:
    runs-on: 'ubuntu-latest'

    steps:
      - uses: 'actions/checkout@v3'

      - uses: 'denoland/setup-deno@v1'
        with:
          deno-version: 'v1.x'
      
      - run: 'deno test --fail-fast'
